require "spec_helper"

RSpec.describe Lab::Crawler do
  it "has a version number" do
    expect(Lab::Crawler::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
